PORT=4100
PLATFORM_ROOT_DOMAIN=localhost
DEFAULT_BRAND_SLUG=

# Vision-capable LLM calls (Product Studio's "Suggest with AI") — get a key
# at https://console.anthropic.com/settings/keys. Leave unset and the
# feature 503s honestly instead of faking a result.
ANTHROPIC_API_KEY=
